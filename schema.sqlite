CREATE TABLE IF NOT EXISTS mbox (
    key text primary key,
    ackkey text unique,
    from_address text,
    subject text,
    date text,
    body text,
    new_from_address text,
    selfrom text,
    recipients text,
    status text,
    schedule text
);
